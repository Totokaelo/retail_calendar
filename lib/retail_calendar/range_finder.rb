module RetailCalendar
  class RangeFinder
    @@season_start = Time.utc(2014, 2, 2, 0, 0, 0)
    @@leap_years = [2018]

    def self.for_month(month, year)

    end

  private

    def self.month_offset_from_season_start(month, year)
    end
  end
end
